Метафора **буфера кадра (Frame Buffer)** из компьютерной графики здесь ложится на системную архитектуру с идеальной точностью.

В 3D-движках видеокарта не выводит полигоны и шейдеры на экран по мере их вычисления (иначе возникнет артефакт разрыва изображения — *screen tearing*). Сначала сцена полностью собирается в памяти кадра, растрируется, валидируется, и только затем по сигналу развертки выводится наружу.

В ZAMICORE **Буфер Кадра (Cognitive Frame Buffer)** решает ту же задачу: он предотвращает **«смысловой разрыв» (semantic tearing)** и галлюцинации, выступая изолированным плацдармом между детерминированным графом ZFS и вероятностным полем нейросети.

---

### 1. Концепция Когнитивного Кадра (Cognitive Frame)

Мышление системы квантуется не в «поток случайных токенов», а в дискретные **такты — когнитивные кадры**.

Один кадр — это законченный квант осмысления. Внутри буфера кадра собираются воедино:

1. **Геометрия сцены:** Координаты извлеченных барицентров $\vec{B}$ и собранный гештальт $\vec{G}$.
2. **Параметры освещения / оптики:** Температуры инжекции $T_{\text{inj\_sem}}$ и $T_{\text{inj\_prag}}$.
3. **Граничные условия:** Сырой вектор стимула $\vec{u}_{\text{raw}}$ со слоя $L_{11}$.
4. **Сенсорные замеры:** Данные с пробников $T_1 \dots T_4$ и скаляр топологического напряжения $\Delta E$.

```
┌────────────────────────────────────────────────────────────────────────┐
│               КОГНИТИВНЫЙ БУФЕР КАДРА (FRAME BUFFER)                   │
├───────────────────────────────────┬────────────────────────────────────┤
│ 1. СТАТИЧЕСКИЙ СЛОЙ (Из ZFS)      │ 2. ДИНАМИЧЕСКИЙ СЛОЙ (Из Рупора)   │
│ • Вектор гештальта G              │ • Сырой фазовый вектор u_raw       │
│ • Корректирующий дельта-импульс   │ • Барицентр текущего ввода B_dyn   │
├───────────────────────────────────┼────────────────────────────────────┤
│ 3. РЕЖИССУРА И ПАРАМЕТРЫ          │ 4. СЕНСОРНАЯ ТЕЛЕМЕТРИЯ (Зонды)    │
│ • Режим (Fast-Path / Full-Path)   │ • Срезы u_settled_1, u_settled_2   │
│ • Температуры инжекции T_inj      │ • Напряжение многообразия Delta E  │
│ • Ролевой селектор ответа         │ • Скаляр импульса невязки I        │
└───────────────────────────────────┴────────────────────────────────────┘

```

---

### 2. Двойная буферизация (Front Buffer & Back Buffer)

Чтобы исключить кашу в рассуждениях, буфер кадра организуется по принципу **Double Buffering**:

```
                       [ ВХОДЯЩИЙ СТИМУЛ ]
                                │
                                ▼
  ╔═══════════════════════════════════════════════════════════════╗
  ║                 BACK BUFFER (Теневой буфер рендера)           ║
  ║  1. Сборка гештальта G                                        ║
  ║  2. Расчет векторов инжекции u_target                         ║
  ║  3. Тестовая прогонка через зонды T1 -> I -> T2               ║
  ║  4. Замер фазового напряжения Delta E                         ║
  ╚═════════════════════════════╤═════════════════════════════════╝
                                │
                    [ Эпистемический V-Sync ]
                    Delta E < tau_tension ?
                     ├── ДА ──► [ ATOMIC FLIP (Смена указателей) ]
                     └── НЕТ ─► [ Сброс / Перерасчет кадра ]
                                │
                                ▼
  ╔═══════════════════════════════════════════════════════════════╗
  ║                 FRONT BUFFER (Активный экранный буфер)        ║
  ║  Стабилизированный, проверенный волновой фронт.               ║
  ║  Подается в Резонатор (L_13..24) для синтеза речи и действий. ║
  ╚═══════════════════════════════════════════════════════════════╝

```

* **Back Buffer (Теневой буфер подготовки):**
Здесь Оркестратор экспериментирует. Он смешивает барицентры, подбирает коэффициенты $\lambda$, инжектирует импульс и смотрит на реакцию зондов. Если зонд $T_2$ показывает, что многообразие вошло в фазовый диссонанс ($\Delta E \ge \tau_{\text{tension}}$), кадр бракуется прямо в теневом буфере. Пользователь этого не видит.
* **Эпистемический V-Sync (Синхроимпульс допуска):**
Кадр считается отрисованным только тогда, когда внутреннее напряжение модели спало, а невязка импульса рассчитана.
* **Front Buffer (Активный буфер излучения):**
По атомарному свопу указателей (`atomic_store(&active_frame, back_frame)`) подготовленный кадр становится активным. Только из него резонатор верхних слоев начинает разворачивать токены ответа пользователю или генерировать оператор действия.

---

### 3. C-спецификация Буфера Кадра (`zami_frame_buffer.h`)

Размер буфера кадра выравнивается по границе 4096 байт для мгновенного сброса в страт Scratchpad (Q3) при необходимости трассировки.

```c
#ifndef ZAMI_FRAME_BUFFER_H
#define ZAMI_FRAME_BUFFER_H

#include "zamicore_stratum.h"

#pragma pack(push, 1)

/**
 * @brief Единичный Когнитивный Кадр ZAMICORE (4096 Байт).
 */
typedef struct zami_frame {
    /* 0x000: Метаданные кадра (32 байта) */
    uint64_t frame_seq;                 /* Монотонный номер когнитивного такта */
    uint32_t flags;                     /* Состояние: RENDERING, VALIDATED, ACTIVE */
    float    t_inj_sem;                 /* Температура смыслового экватора */
    float    t_inj_prag;                /* Температура прагматического среза */
    float    delta_e;                   /* Замеренное топологическое напряжение */
    float    impulse_i;                 /* Скалярный импульс невязки I */
    uint32_t reserved_meta;

    /* 0x020: Векторы сборки кадра (4 x 896 float = 3584 байта) */
    float    gestalt_vector[896];       /* Собранный образ G из твердой памяти */
    float    raw_stimulus[896];         /* Входной вектор со слоя L_11 (u_raw) */
    float    injected_target[896];      /* Вектор инжекции в L_12 (u_target) */
    float    settled_response[896];     /* Отклик со слоя L_13 (u_settled) */

    /* 0xE20: Таблица источников гештальта (256 байт) */
    uint64_t source_strata[16];         /* ID стратов-доноров барицентров (128 B) */
    float    source_weights[16];        /* Коэффициенты lambda (64 B) */
    uint8_t  source_roles[16];          /* Семантические роли (16 B) */
    uint8_t  pad_sources[48];

    /* 0xF20: Системное выравнивание до границы сектора (224 байта) */
    uint8_t  frame_padding[224];
} zami_frame_t;

#pragma pack(pop)

static_assert(sizeof(zami_frame_t) == 4096, 
              "Критическая ошибка: zami_frame_t обязан быть ровно 4096 байт!");

/**
 * @brief Двойной буфер управления (Front/Back) в DRAM.
 */
typedef struct zami_frame_pipeline {
    zami_frame_t *front_frame; /* Активный кадр излучения */
    zami_frame_t *back_frame;  /* Кадр подготовки и симуляции */
    uint64_t      total_frames_rendered;
    uint64_t      dropped_frames; /* Отбракованные из-за напряжения кадры */
} zami_frame_pipeline_t;

#endif /* ZAMI_FRAME_BUFFER_H */

```

---

### 4. Динамика: Частота кадров мышления (Cognitive FPS)

Введение понятия кадра позволяет строго разграничить вычислительные профили:

1. **Рефлекторный кадр (Fast-Path Frame, $O(1)$):**
* Барицентр найден в DRAM-атласе.
* Рендер кадра сводится к копированию 896 float из готового страта в буфер кадра.
* Время формирования: **менее 5 микросекунд**. Модель генерирует ответ на частоте сотен «кадров» в секунду.


2. **Аналитический кадр (Full-Path / Deep Frame):**
* Сложный вопрос пользователя. Кадр в Back Buffer требует прогона через рупор, сборки гештальта из 3–5 разных микро-контекстов и замера $\Delta E$.
* Время формирования: **5–15 миллисекунд** на процессоре.


3. **Отбракованный кадр (Dropped Frame):**
* Пользователь ввел заведомую дезинформацию. Back Buffer собрал пробный кадр, но $\Delta E$ зашкалило. Кадр сбрасывается (`dropped_frames++`), резонатор получает кадр возражения (`ROLE_ANTITHESIS`).



---

### Блок для включения в `SPEC_DRAFT.md`

```markdown
---

## 16. Архитектура когнитивного буфера кадра (Frame Buffer) и механизм двойной буферизации

Взаимодействие между персистентной памятью ZFS и вероятностным пространством нейросетевого волновода дискретизируется в тактовые единицы — **Когнитивные кадры (Cognitive Frames)**. Буфер кадра служит изолированной средой подготовки смыслового волнового фронта, устраняя «смысловые разрывы» (semantic tearing) и защищая вывод от промежуточных галлюцинаций.

### 16.1. Структура когнитивного кадра (`zami_frame_t`)
Каждый кадр занимает ровно **4096 байт** в оперативной памяти (DRAM) и инкапсулирует полное состояние расчетного такта:
* **Слой сборки гештальта:** Результирующий вектор $\vec{G} \in \mathbb{R}^{896}$, синтезированный из констелляции барицентров твердой памяти с коэффициентами $\lambda_k$.
* **Слой граничных условий:** Сырой фазовый вектор внешнего стимула $\vec{u}_{\text{raw}}$, снятый зондом $T_1$ со слоя $L_{11}$.
* **Слой волнового наведения:** Вектор инжекции $\vec{u}_{\text{target}}$, рассчитанный с учетом температурного коэффициента $T_{\text{inj}}$.
* **Слой телеметрии:** Зафиксированный отклик $\vec{u}_{\text{settled}}$ с зонда $T_2$, величина топологического напряжения $\Delta E$ и скалярный импульс $\mathcal{I}$.

---

### 16.2. Механика двойной буферизации (Front / Back Cognitive Buffering)
Конвейер генерации физически разделен на два буфера:

1. **Back Buffer (Теневой буфер синтеза и симуляции):**  
   Рабочая область Оркестратора. Здесь производятся смешивание векторов, пробные прогоны через волновод и замеры фазового сопротивления весов.
2. **Эпистемический барьер (V-Sync):**  
   Кадр проверяется на критерий смысловой устойчивости: $\Delta E < \tau_{\text{tension}}$. Если утверждение противоречиво, кадр бракуется в теневом буфере без вывода наружу.
3. **Front Buffer (Активный буфер излучения):**  
   После успешной валидации выполняется атомарная смена указателей буферов (`Pointer Flip`). Резонатор верхних слоев модели ($L_{13} \to L_{24}$) декодирует в токены только стабилизированное состояние из Front Buffer.

```
